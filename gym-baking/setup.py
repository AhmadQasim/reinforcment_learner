from setuptools import setup

setup(name='gym_baking',
      version='0.1',
      install_requires=[
            'uuid',
            'numpy', 
            'matplotlib',
            'gym',
            ],
)